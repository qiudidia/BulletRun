# 2026-06-14 工作记录

## Bullet Run - 2D俯视角射击游戏项目搭建与修复

### 完成内容

1. **项目基础结构**
   - 创建 `project.godot`（Godot 4，窗口1280x720）
   - 注册 `GameSettings` 为 Autoload 单例
   - 创建完整目录结构

2. **开场动画** — 已修复
   - 原 `TextureRect` 无贴图导致空白，改为 `Label` 节点显示文字
   - 三阶段：VeeStudio → Godot Engine → BULLET RUN
   - 添加点击左键跳过功能

3. **主菜单** — 已修复
   - 修正7个 `@onready` 节点路径（漏了中间 `VBox` 层）
   - 标题呼吸缩放效果

4. **设置界面** — 已补全
   - 四类设置：视频/音频/控制/游戏
   - 新增：语言选项（中文/English）
   - 新增：按键绑定UI（点击按钮后按新键）
   - 新增：准星预览（十字/圆点/圆环，实时绘制）
   - 所有设置自动保存到 `user://settings.cfg`

5. **核心游戏系统**
   - 玩家控制器：WASD移动、鼠标瞄准、左键射击、R换弹、1/2切武器
   - 子弹系统：`Bullet.tscn` 场景（含碰撞体和可视化）
   - FPS显示（右上角，可开关）
   - 无帧率限制

6. **BOT对战模式** — 已修复
   - 创建 `Bot.tscn` 场景文件（含Sprite2D、WeaponPivot、ShootTimer、NavigationAgent2D）
   - 重写 `BotAI.gd`：正确使用场景节点，射击改为实例化 `Bullet.tscn`
   - BotGame 场景正确生成BOT

7. **僵尸波次模式** — 已修复
   - 创建 `Zombie.tscn` 场景文件
   - 重写 `ZombieAI.gd`：正确使用场景节点，近战攻击逻辑

### Bug修复记录

- **E001**: `Player.gd` 引用 `$AnimatedSprite2D` → 改为 `$Sprite2D`
- **E002**: `Sprite2D` 不支持 `.play()` → 改为 `modulate`
- **E003**: `preload().new()` 语法错误 → 改为条件分支
- **E004**: `Intro.gd` `await` 在 `_process` 中每帧触发 → 添加标志位
- **E005**: `.tscn` 文件尾部 `]` 格式错误 → 移除
- **E006**: `Player.tscn` CanvasLayer HUD 重复 → 移除，改由游戏场景管理
- **E007**: HUD 引用 null → 全部加 null 安全检查
- **E008**: AI动态创建时缺少节点 → 创建 `.tscn` 场景文件彻底解决
- **E009**: `weapon_pivot` null → 加 null 检查
- **E010**: `attack_timer` null → 加 null 检查
- **E011**: 商店按钮未连接信号 → 在 `_ready()` 中连接
- **E012**: `VSyncMode` 传参顺序错误 → 修正 `window_set_vsync_mode` 调用
- **E013**: 主菜单节点路径少 `VBox` 层 → 7个路径全部修正
- **E014**: `Intro.tscn` TextureRect 无贴图 → 改为 Label 显示文字
- **E015**: `Player.gd` `$CanvasLayer/...` 引用不存在的节点 → 改为 null 安全模式
- **E016**: `Player.gd` `_ready()` 直接用 `health_bar` 未判空 → 加 null 检查
- **E017**: `Player.gd` `set_collision_mask_value(2, true)` 设错层 → 删掉，由场景管理
- **E018**: `Bullet.tscn` 碰撞掩码默认为1 → 改为5（能碰layer1玩家和layer4敌人）
- **E019**: 缺失 `Bullet.tscn`、`Bot.tscn`、`Zombie.tscn` → 全部创建
- **E020**: `Bot.tscn`/`Zombie.tscn` 可视化节点命名 `Visual` 与脚本 `$Sprite2D` 不匹配 → 改名为 `Sprite2D`
- **E021**: `BotAI.gd:65` `global_position()` 误作函数调用 → 去掉 `()`（`global_position` 是属性）
- **E022**: `Settings.gd:147` `InputMap.get_action_list()` Godot 3 写法 → 改为 Godot 4 的 `InputMap.action_get_events()`
- **E023**: `settings.tscn` 节点名 `"BGML Abel"` 含空格 → 修正为 `"BGMLabel"`
- **E024**: `intro.tscn` Godot 阶段只有文字无图标 → 重写为 `CenterContainer` + `TextureRect`（加载 `assets/godot_logo.svg`）+ 文字
- **E025**: `Settings.gd:206` `language` 设置读取类型不匹配 → `GameSettings` 默认值是 `"zh"`（字符串），但脚本变量声明为 `int` → 改为动态类型转换
- **E026**: 多个 `.tscn` 文件 `sub_resource` 定义在引用之后 → Godot 4 要求先定义后引用。修复 `Player.tscn`、`Bullet.tscn`、`Bot.tscn`、`Zombie.tscn`
- **E027**: `zombie_game.tscn` 末尾多余 `]` → 删除
- **E028**: `Settings.gd:150` `InputEventMouseButton` 强制转为 `InputEventKey` → 增加类型判断，支持键盘和鼠标按钮显示
- **E029**: `bot_game.tscn` / `zombie_game.tscn` `Player` 节点 `parent=""` → 导致场景出现双根节点，加载崩溃 → 修正为 `parent="."`

### 文件清单

```
scenes/intro/intro.tscn         ✅ 开场动画场景
scripts/Intro.gd                ✅ 开场动画脚本
scenes/menu/main_menu.tscn     ✅ 主菜单场景
scripts/MainMenu.gd             ✅ 主菜单脚本
UI/settings.tscn               ✅ 设置界面场景（已补全）
UI/Settings.gd                 ✅ 设置界面脚本（已补全）
Autoload/GameSettings.gd        ✅ 全局设置管理器
scenes/Player.tscn              ✅ 玩家场景
scripts/Player.gd                ✅ 玩家控制器
scenes/game/Bullet.tscn          ✅ 子弹场景
scripts/Bullet.gd                ✅ 子弹脚本
scenes/game/bot_mode/Bot.tscn          ✅ BOT场景
scripts/BotAI.gd                       ✅ BOT AI脚本
scenes/game/bot_mode/bot_game.tscn   ✅ BOT对战场景
scenes/game/bot_mode/BotGame.gd      ✅ BOT对战控制器
scenes/game/zombie_mode/Zombie.tscn      ✅ 僵尸场景
scripts/ZombieAI.gd                     ✅ 僵尸AI脚本
scenes/game/zombie_mode/zombie_game.tscn ✅ 僵尸波次场景
scenes/game/zombie_mode/ZombieGame.gd  ✅ 僵尸波次控制器
```

### 操作方式

| 操作 | 按键 |
|------|------|
| 移动 | WASD |
| 瞄准 | 鼠标 |
| 射击 | 左键 |
| 换弹 | R |
| 切武器 | 1 / 2 |
| 跳过动画 | 左键 |

### 项目路径
`c:\Users\32577\Desktop\game\Bullet Run`
