# 2026-06-29 工作日志

## 连杀奖励系统全面修复（上午）
- **根本原因**: 敌人组名是 `"enemy"`（单数），但炮击/爆炸桶AOE查的是 `"enemies"`/`"zombies"`/`"bots"`（复数），导致所有AOE伤害打不到敌人
- **连杀触发修复**: `_check_kill_streak()` 从 `==` 改为 `>=`，防止手榴弹双杀跳过3/5/10
- **狂暴模式修复**: Timer 加 `process_mode = ALWAYS`，暂停时也能计时；8秒后自动关闭 `player.infinite_ammo`
- **StreakPanel修复**: 所有按钮加 `mouse_filter=IGNORE` + `focus_mode=FOCUS_NONE`
- **连杀奖励系统已彻底删除**: 用户说太辣鸡，两个模式都删了

## 爆炸桶修复
- 改为 `extends StaticBody2D`，子弹用 `body_entered` 直接检测
- `_apply_aoe()` 组名统一为 `"enemy"`（单数）
- 碰撞体积恢复 40x40
- 生成时检查掩体重叠（30次尝试，60px缓冲区）

## Bot 不动问题修复
- 巡逻点从绝对坐标改为相对于当前位置(-300~300)
- 加防卡住检测：1秒内移动不到5像素就换巡逻方向+重新找玩家

## 联机系统实现（下午）
新增文件：
- `Autoload/NetworkManager.gd` — 网络管理器（ENet连接、UDP广播房间发现）
- `scripts/LobbyUI.gd` — 联机大厅UI（创建/加入/等待）
- `scripts/MultiplayerPlayer.gd` — 联机玩家控制器（authority + RPC同步）
- `scripts/DuelMap.gd` — 单挑小地图(1600x1600)
- `scripts/BrawlMap.gd` — 乱斗中地图(2000x2000)
- `scenes/multiplayer/Lobby.tscn` — 大厅场景
- `scenes/game/multiplayer/` 下：duel/brawl/zombie_coop 三个游戏场景+脚本

修改文件：
- `scripts/Bullet.gd` — 新增 `owner_peer_id`，联机友军保护（不打自己、僵尸不打队友、单挑不打同队）
- `scripts/Grenade.gd` — 新增 `owner_peer_id`，PvP模式手榴弹伤害玩家、友军保护
- `scripts/MainMenu.gd` — 新增联机模式卡片
- `scenes/menu/main_menu.tscn` — 新增 MultiplayerCard 节点
- `Autoload/GameSettings.gd` — 新增约30个联机翻译键
- `project.godot` — 注册 NetworkManager autoload

## 联机系统 bug 修复（第二轮）
1. Python `"""` docstrings → `#` 注释（GDScript不支持`"""`）
2. 菜单路径修正 `scenes/main_menu` → `scenes/menu`
3. 远程玩家 `died` 信号未连接 → 已添加 lambda 连接
4. 远程玩家 `move_and_slide()` 与 `position.lerp()` 冲突 → 移除远程玩家的 `move_and_slide()`
5. `_send_room_info` 和 `_notify_game_start` RPC `any_peer` → `authority`
6. `_stop_broadcast_client()` 不存在 → 改用 `stop_discovery()`
7. `GameSettings.t("kills")` 格式重复 → 改为 `GameSettings.t("kills", [kills])`
8. 僵尸联机远程玩家死亡不触发 game_over → 新增 `remote.died.connect(func(): _game_over())`

## 联机系统 bug 修复（第三轮）
1. DuelGame 死亡面板3秒后重生但不消失 → `_respawn_local_player()` 中添加 `death_panel.queue_free()`
2. 远程玩家重生后恢复 `set_process_input(true)` → 改为只有 `is_local_player` 才恢复输入
3. ZombieCoopGame `_on_local_health_changed` 中 `new_hp<=0` 重复触发 `_game_over()` → 移除，由 `_on_local_died()` 统一触发
4. ZombieCoopGame 客户端商店按钮点击无效 → 客户端通过 `_request_shop_buy.rpc_id(1)` 请求房主购买
5. ZombieCoopGame 客户端商店打开不暂停 → `_sync_shop_open` 中添加 `get_tree().paused = true`
6. ZombieCoopGame 客户端商店关闭不取消暂停 → `_sync_shop_close` 中添加 `get_tree().paused = false`
7. `_spawn_bullet` / `_spawn_bullet_at` / `_throw_grenade` 中 `get_tree().current_scene.add_child` 可能不安全 → 添加 null 检查
8. Bullet.gd 子弹友军保护 → 新增：不打发射者自己、僵尸模式不打其他玩家、单挑不打同队
9. Grenade.gd 手榴弹PvP伤害 → 联机PvP模式手榴弹伤害其他玩家（但不打自己/队友）
10. `_sync_shop_upgrade` 缺少金钱参数 → 增加 `current_money` 参数让客户端更新金钱显示

## 联机商店购买流程重构（晚上）
- **问题**: 之前 bug 修复把客户端购买改成请求房主执行，这是完全错误的设计——联机合作模式每个玩家应该自己花钱自己买
- **修改**: ZombieCoopGame.gd 商店系统重构
  1. 删除 `_request_shop_buy` RPC（客户端不再请求房主购买）
  2. 删除 `_sync_shop_upgrade` RPC（不再同步升级结果）
  3. 删除 `_sync_unlock_weapon` RPC（每个玩家本地解锁武器）
  4. 所有 `_on_*_upgrade/buy` 函数改为纯本地操作——直接从本地 money 扣款，直接对 local_player 施加效果
  5. `_open_shop` 只发送 wave_reward 金额给客户端，不再发送房主的总金钱（客户端把奖励加到自己的 money 上）
  6. `_close_shop` 添加 `is_host` 检查——只有房主能关闭商店并开始下一波
  7. `_on_enemy_died` 添加 `_sync_kill_reward.rpc()` ——合作模式所有玩家共享击杀奖励
  8. Continue 按钮：客户端禁用并显示"等待房主开始..."，房主正常显示"下一波"

## 解析错误修复
- NetworkManager.gd: `startswith` → `begins_with`（GDScript用begins_with而非Python的startswith）
- DuelGame.gd: 空函数体 `func _respawn_player(): # comment` → 加 `pass`
- MultiplayerPlayer.gd: `aim_dir` 在 `if bullet:` 块内声明，块外 RPC 用不到 → 提升到函数顶层声明
- LobbyUI.gd: `ModeDesc`/`ModeLabel` 用 `get_node` 找不到嵌套子节点 → 改为类变量直接引用
- `name` 遮蔽 `Node.name` → 改名 `r_name`/`room_name_str`
- 未使用参数加 `_` 前缀

## 玩家昵称 + 自由选色系统
- **NetworkManager.gd**: 新增 `player_name`(本地昵称)/`player_names`(peer_id→昵称)/`zombie_colors`(peer_id→颜色名)
  - `_register_player_name` RPC: 客户端连接后注册昵称到房主
  - `_sync_player_names` RPC: 房主同步昵称到所有客户端
  - `set_brawl_color`/`set_zombie_color`: 新增，允许乱斗/僵尸模式玩家自选颜色
  - `get_player_display_name`: 返回昵称而非 ID
  - `get_color_options`: 返回当前模式可选颜色列表
  - `_send_room_info` 改为同步 duel_teams/brawl_colors/zombie_colors/player_names 四个字典
  - TEAM_COLORS 新增 "green"
- **LobbyUI.gd**: 创建/加入界面各加一行昵称输入框(LineEdit)；等待界面改为动态颜色按钮
  - `_on_color_selected` 统一处理所有模式的选色
  - `_update_color_buttons` 根据模式动态创建颜色按钮
  - `_update_color_highlight` 已选颜色高亮/被占颜色灰显禁用
  - `_sync_colors` RPC 同步所有模式的颜色字典
  - `_has_color_conflict` 检查多人颜色冲突
  - `_update_start_button` 乱斗/僵尸模式检查颜色冲突才能开始
  - 玩家列表显示昵称而非 ID
- **GameSettings.gd**: 新增翻译 `green_team`/`your_nickname`/`enter_nickname`/`default_nickname`/`select_your_color`/`color_conflict_warning`

## NoRoomLabel修复 + 僵尸模式不选色 + 必填字段 + disconnect_network恢复
- LobbyUI.gd: `NoRoomLabel` 改为类变量 `no_room_label` 直接引用
- 僵尸合作模式隐藏颜色选择容器，玩家列表不显示颜色标签
- 创建/加入按钮初始禁用，昵称+房间名非空才启用创建，昵称非空才启用加入
- `disconnect_network()` 函数恢复（被误删）
- 取消按钮文字：房主"解散房间"，客户端"离开房间"

## 准备系统 + 踢人系统
- **NetworkManager.gd**: 新增 `player_ready`字典(peer_id→bool)、`player_ready_changed`信号
  - `toggle_ready()`: 本地切换准备，房主直接sync，客户端RPC请求房主
  - `is_local_ready()`: 返回本地玩家准备状态
  - `all_ready()`: 所有玩家都准备才返回true
  - `_request_ready`/`_sync_ready_states` RPC: 客户端请求+房主同步
  - `kick_player(peer_id)`: 房主踢人，先发`_notify_kicked` RPC再disconnect_peer
  - `_notify_kicked` RPC: 被踢客户端收到通知→触发host_disconnected
  - `_send_room_info` 新增 `player_ready` 参数
- **LobbyUI.gd**: 新增 `ready_btn`(准备/取消准备按钮)、`kick_hbox`(踢人按钮行)
  - `_update_ready_button`: 准备后变绿色"取消准备"，未准备变灰色"准备"
  - `_update_player_list`: 显示每个玩家[已准备/未准备]状态
  - 房主看到踢人按钮行（每个其他玩家旁边一个"踢出 xxx"按钮）
  - `_update_start_button`: 先检查`all_ready()`，全部准备后再检查颜色/队伍冲突
