# 2026-06-28 工作日志

## 联机系统 bug 全面检查与修复

### 发现的 bug（按严重程度）

**致命 bug：**
1. ~~Python `"""` docstring 在新文件中使用~~ → 已全部替换为 `#` 注释（涉及 NetworkManager.gd、LobbyUI.gd、MultiplayerPlayer.gd、DuelGame.gd、BrawlGame.gd、ZombieCoopGame.gd、MainMenu.gd）
2. ~~菜单路径错误~~ → `res://scenes/main_menu/main_menu.tscn` 改为 `res://scenes/menu/main_menu.tscn`（涉及 LobbyUI.gd、DuelGame.gd、BrawlGame.gd、ZombieCoopGame.gd）

**严重 bug：**
3. ~~远程玩家 `died` 信号未连接~~ → DuelGame/BrawlGame 中 `_add_remote_player()` 新增 `remote.died.connect(func(): _on_remote_died(peer_id))`
4. ~~远程玩家 `move_and_slide()` 与 `position.lerp()` 冲突~~ → MultiplayerPlayer.gd 中远程玩家部分移除 `velocity = _velocity` 和 `move_and_slide()`
5. ~~RPC 权限错误~~ → `_send_room_info` 和 `_notify_game_start` 从 `any_peer` 改为 `authority`
6. ~~`_stop_broadcast_client()` 函数不存在~~ → `join_room()` 中改为调用已有的 `stop_discovery()`

**中等 bug：**
7. ~~翻译格式重复~~ → DuelGame/BrawlGame 的 kills/deaths 从 `GameSettings.t("kills") + ": %d" % kills` 改为 `GameSettings.t("kills", [kills])`
8. ~~僵尸联机远程玩家死亡不触发 game_over~~ → `_add_remote_player()` 新增 `remote.died.connect(func(): _game_over())`

**轻微问题（已修复）：**
9. ~~MultiplayerPlayer.gd 屏幕震动 else 块中的死代码~~ → 移除嵌套 else 中永远为 null 的 camera 检查
