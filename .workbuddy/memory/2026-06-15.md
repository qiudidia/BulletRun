# 2026-06-15 工作记录

## Bullet Run - Bug修复与资源替换

### 完成内容

1. **Bug修复**
   - `BotAI.gd`：删除未使用的 `sprite` 变量（原类型标注 `Node2D` 与实际 `ColorRect` 冲突）
   - `ZombieAI.gd`：去掉 `sprite` 变量的 `Node2D` 类型标注，修复赋值类型错误
   - `Player.gd`：`ammo_changed` 信号声明了但未 emit，在 `_update_ammo_display()` 中补上 emit
   - `Settings.gd`：`_get_action_key_text` 强制转 `InputEventKey` → 加类型判断，支持鼠标按钮显示
   - `bot_game.tscn` / `zombie_game.tscn`：Player 节点 `parent=""` → 修正为 `parent="."`（双根节点崩溃）

2. **Godot 开场 Logo 替换**
   - 将用户提供的 `C:\Users\32577\Desktop\game\logo_dark.svg` 复制到项目 `assets/logo_dark.svg`
   - `intro.tscn` 改为引用真实 Godot logo，去掉旁边多余的文字 Label，图片尺寸 320×120
   - 删除旧的自制 `assets/godot_logo.svg`

### Bug修复记录（续）

- **E028**: `Settings.gd` `InputEventMouseButton` 强制转 `InputEventKey` → 加类型分支
- **E029**: `bot_game.tscn` / `zombie_game.tscn` `parent=""` → `parent="."` 修复双根崩溃
- **E030**: `BotAI.gd` / `ZombieAI.gd` `sprite: Node2D` 类型标注错误（实际是 ColorRect）→ 删除或去掉类型标注
- **E031**: `Player.gd` `ammo_changed` 信号未 emit → 在 `_update_ammo_display` 中补上

---

## 2026-06-15 第二轮：补全 Bot 模式和僵尸模式

### 完成内容

1. **创建地图系统** - `scripts/GameMap.gd`
   - 自动生成 `NavigationRegion2D` + `NavigationPolygon`（矩形可行走区域）
   - 生成随机障碍物（StaticBody2D + NavigationObstacle2D）
   - 挂到 `bot_game.tscn` 和 `zombie_game.tscn` 的 `Map` 节点上

2. **修复角色颜色**
   - `Bot.tscn`：颜色从蓝色 `(0.2,0.4,0.9)` 改为红色 `(0.9,0.2,0.2)`（敌方）
   - `Zombie.tscn`：保持绿色 `(0.2,0.7,0.2)`（僵尸）
   - 玩家 `Player.tscn`：`Sprite2D` 使用 `icon.svg`，颜色正常

3. **添加自动换弹** - `Player.gd`
   - 在 `_shoot()` 末尾检查：如果 `current_ammo <= 0` 且不在换弹，自动调用 `_start_reload()`

4. **重写 ZombieAI.gd**
   - 修复导航 bug：去掉 `velocity_computed` 信号，改用 `get_next_path_position()` 直接移动
   - 去掉 `look_at()`（会旋转整个身体导致导航方向错乱）
   - 正确设置 `navigation_agent.target_position` 和 `path_desired_distance`

5. **重写 BotAI.gd**
   - 修复导航 bug：同样改用 `get_next_path_position()`
   - 添加状态机：PATROL → CHASE → SHOOT → COVER（换弹时找掩体）
   - 修复换弹逻辑：弹药打完自动换弹并进入 COVER 状态

### Bug 修复记录（第二轮）

- **E032**: `GameMap.gd` `NavigationPolygon` 生成方式错误 → 用 `add_outline()` + `make_polygons_from_outlines()` 正确生成可行走区域
- **E033**: `ZombieAI.gd` / `BotAI.gd` 导航不工作 → 重写移动逻辑，使用 `get_next_path_position()`
- **E034**: `Player.gd` 无自动换弹 → 在 `_shoot()` 末尾添加自动换弹逻辑
- **E035**: `Bot.tscn` 颜色错误（蓝色应为敌方红色）→ 修改 ColorRect 颜色

### 文件清单

- 新建：`scripts/GameMap.gd`
- 修改：`scripts/Player.gd`、`scripts/BotAI.gd`、`scripts/ZombieAI.gd`
- 修改：`scenes/game/bot_mode/Bot.tscn`、`scenes/game/bot_mode/bot_game.tscn`
- 修改：`scenes/game/zombie_mode/zombie_game.tscn`
