# 2026-06-30 工作日志

## 联机僵尸模式死亡观战系统 + BOSS手榴弹

### 1. BOSS 扔手榴弹（单机+联机）
- **ZombieAI.gd**: 添加 `_boss_grenade_timer`，BOSS 每 8 秒向玩家方向投掷手榴弹（带预判），伤害 60，爆炸半径 100，引信 1.5s
- **Grenade.gd**: 添加 `is_enemy_grenade` 标志。敌人手榴弹伤害玩家（而非僵尸），外观暗红色
- **ZombieCoopGame.gd**: 添加 `_sync_enemy_grenade` RPC，BOSS 手榴弹爆炸时同步位置到客户端，客户端检查本地玩家是否在范围内并扣血
- 单机模式 `NetworkManager.connected=false`，不调用 RPC，直接伤害玩家

### 2. 联机死亡观战系统（仅 ZombieCoopGame.gd）
- 玩家死亡不再直接结束全队游戏
- 死亡面板显示"你撑到了第X波"+"还有X个队友正在战斗"+[观战][退出游戏]按钮
- **观战**: 摄像机 `top_level=true` 脱离死亡玩家，`_process` 中跟随存活队友；按空格切换目标
- **全员阵亡**: 显示"全员阵亡！"+[返回主菜单]
- `_check_all_dead()` 检查所有玩家（本地+远程）是否全死，是则触发 `_game_over()`
- `_on_remote_player_died()` 替代原来直接 `_game_over()` 的连接

### 3. ZombieAI 目标选择优化
- `_find_player()` 优先选存活玩家，跳过 `dead==true` 的
- `_physics_process()` 检测当前目标死亡后自动重新寻找

### 4. 新翻译键（GameSettings.gd）
- `survived_to_wave`, `teammates_fighting`, `spectate`, `exit_game`, `all_dead`, `spectating_label`, `spectate_switch_hint`

## 新账号接管项目 + 游戏认知梳理

### 背景
- 原账号积分耗尽，本项目改由当前 WorkBuddy 账号接管。
- 接管后第一件事：清理超限的 MEMORY.md（92行→精简重写），并全面认识游戏架构。

### 认知梳理（代码库深度调研结果）
- **启动流程**: intro.tscn(Intro.gd 三阶段Logo动画,左键跳过) → main_menu.tscn(MainMenu.gd,开始游戏展开模式选择) → 三模式: bot_game.tscn / zombie_game.tscn / Lobby.tscn(联机,按room_mode跳duel/brawl/zombie_coop)。
- **四武器系统**(修正记忆): 实际是手枪(0)/步枪(1)/狙击枪(2)/刀(3)，weapon_4(键4)对应刀。视觉全代码绘制无外部图。
- **scripts/ 21个脚本清单**: Intro/MainMenu/Player(1271行)/BotAI(1007行)/ZombieAI(490行)/Shop/ShopUI/Bullet/Grenade/GameMap/DuelMap/BrawlMap/MultiplayerPlayer(1130行)/LobbyUI/Console/Minimap/Barrel/WeaponIcon/PauseInputHandler/KillStreakUI(死代码)/VeeIcon。

### 接管时发现的待修复问题（已记入 MEMORY.md）
1. KillStreakUI.gd 遗留死代码——依赖 Player.gd 未定义的 kill_streak_updated 信号，永不工作(连杀奖励已删但此文件未清理)。
2. DuelGame.gd `_respawn_player()` 函数体为 `pass`——联机单挑重生同步未实现。
3. 翻译键缺失——DuelGame.gd 用 `t("back_to_menu")`，翻译表无此键。
4. 代码重复——MultiplayerPlayer.gd 几乎复制 Player.gd；_spawn_blood_vfx 两模式重复；暂停菜单各模式各自创建。
5. Console.gd(`~`键控制台) 仅 Bot/Zombie 接入，联机未接入。

## 修复: GDScript 参数 shadowing `Node.ready` 信号
- 文件: `Autoload/NetworkManager.gd` 第469行
- 原代码: `func _request_ready(ready: bool)` → 参数名 `ready` 与 `Node.ready` 信号冲突
- 修复: 参数改名为 `value`
- 状态: 已修复，无其他同类问题

## 修复: 刀挥刀无动画
- 文件: `scripts/Player.gd`
- 问题: `_knife_attack()` 只有伤害检测和音效，完全没有视觉动画，玩家看不到挥刀动作
- 修复:
  1. 添加 `_play_knife_swing()`：用 `create_tween()` 让刀本身快速旋转摆动（蓄力后拉 50° → 0.04s → 前挥 60° → 0.07s → 回正 0° → 0.06s），总时长 0.17s
  2. 添加 `_spawn_knife_trail(range_val)`：在 weapon_pivot 下创建扇形 Polygon2D（120° 弧、半径=攻击范围、半透明冰蓝色），0.18s 渐变消失，z_index=-1 在武器下方
  3. 添加 `_knife_attack_tween` 变量，防止连按时 tween 冲突
- 状态: 已修复

## 修复: 联机僵尸模式多项问题
- 文件: `scripts/MultiplayerPlayer.gd` + `scenes/game/multiplayer/ZombieCoopGame.gd` + `scripts/LobbyUI.gd` + `Autoload/GameSettings.gd`

### 1. `_sync_position` 未使用警告
- `MultiplayerPlayer.gd` 第99行: 删除未使用的 `_sync_position: Vector2` 变量（实际位置同步使用 `_target_position`）

### 2. `_w_idx` 参数未使用警告
- `MultiplayerPlayer.gd` 第272行: `_sync_shoot(pos, dir, speed, damage, w_idx)` → `w_idx` 改为 `_w_idx`

### 3. anchors 警告（ZombieCoopGame.gd:186,197）
- `spectate_label` 和 `spectate_hint_label` 使用 `set_anchors_and_offsets_preset(PRESET_TOP_WIDE)` 后又设置 `position`/`size`。
- 修复: 改用 `offset_top`/`offset_bottom` 替代 `position`/`size`

### 4. 联机僵尸模式单人就能开始游戏
- `LobbyUI.gd` 第718-725行: 僵尸模式原来没有检查 `player_count < 2`，`player_count > 1` 只在颜色冲突检查时才用。
- 修复: 添加 `if NetworkManager.player_count < 2` 检查，和乱斗模式一样禁用开始按钮并显示"等待玩家"

### 5. 武器UI缺失
- `MultiplayerPlayer.gd` `_ready()` 第156行: `_create_weapon_ui()` 在 `is_local_player = is_multiplayer_authority()`（第159行）之前被调用，此时 `is_local_player` 还是 `false`，导致 `_create_weapon_ui()` 直接 `return`。
- 修复: 将 `_create_weapon_ui()` 移到 `is_local_player` 设置之后（第165行）。远程玩家时函数仍会 `return`，然后后面的清理代码会删除远程玩家的 crosshair。

### 6. 视野太大（Camera zoom 未设置）
- `ZombieCoopGame.gd` `_create_player_node()`: 代码动态创建 Camera2D 但没有设置 `zoom`，默认 `Vector2(1, 1)`。而 `Player.tscn` 中 Camera2D 的 zoom 是 `1.5`。
- 修复: 在 `_create_player_node()` 中设置 `camera.zoom = Vector2(1.5, 1.5)`

### 7. 左下角队友信息面板（新增）
- `ZombieCoopGame.gd` 新增:
  - `_create_teammate_panel()`: 在 ui_layer 左下角创建 PanelContainer，内含标题 + 分隔线 + VBoxContainer
  - `_update_teammate_panel()`: 每帧在 `_process()` 中调用，同步添加/移除条目，更新血量条颜色（绿→黄→红）和死亡状态
  - `_add_teammate_entry(peer_id)`: 为每个远程玩家创建 VBoxContainer（名字Label + 血条ProgressBar）
  - `_remove_teammate_entry(peer_id)`: 清理离开玩家的条目
- `GameSettings.gd`: 添加翻译键 `"teammates": {"zh": "队友", "en": "Teammates"}`
- 状态: 全部已修复

## 创建: Bullet Run 游戏官网
- 时间: 14:44
- 内容: 为 7.11 正式上线创建官方网站
- 素材复用: `assets/logo_dark.svg` (游戏logo)、`assets/vee_logo.svg` (工作室logo)、`icon.svg` (favicon)、`assets/qiudidia.jpg` (创始人)
- 网站结构:
  - Hero: 大标题"死斗:001 / BULLET RUN" + 7.11 上线倒计时 + 平台信息
  - 游戏模式: Bot对战 / 僵尸合作 / 联机对战 三卡片
  - 特色功能: 四武器系统、智能AI、内置商店、观战系统、代码即美术、低延迟联机
  - 关于我们: Vee Studio 介绍
  - 页脚
- 设计: 暗色科技主题 (#0b0b12 背景 + #ffc93c 金色强调色)，Canvas 粒子背景，Orbitron + Noto Sans SC 字体，响应式布局
- 部署: CloudStudio sandbox 静态托管
- 文件路径: `website/index.html`, `website/css/style.css`, `website/js/main.js`, `website/assets/*`
- 状态: 已部署预览

## 官网修正: 删除"死斗:001"并改为蓝奏云下载
- 时间: 14:55-15:05
- 修正内容:
  1. 删除所有 "死斗:001" 文字，游戏名统一为 "Bullet Run"
  2. 移除 Steam 相关文字，改为 "蓝奏云下载"
  3. 倒计时结束后自动弹出下载弹窗：显示"前往蓝奏云下载"按钮 + 提取密码 h86r（可复制）
  4. Hero 下载按钮在倒计时期间隐藏，结束后显示
- 蓝奏云链接: https://wwbbw.lanzouv.com/b019vyxpej
- 提取密码: h86r

## 功能: 单机模式玩家自定义皮肤 (wanjia.png)
- 时间: 16:31
- 文件: `scripts/Player.gd`, `scenes/game/bot_mode/BotGame.gd`, `scenes/game/zombie_mode/ZombieGame.gd`
- 新增文件: `assets/wanjia.png`, `assets/wanjia_black_transparent.gdshader`
- 实现:
  1. `Player.gd` 添加 `use_custom_skin: bool` 变量和 `_apply_custom_skin()` 方法
  2. 启用时：隐藏 Polygon2D 圆形身体 + 删除 EyeLeft/EyeRight/PupilLeft/PupilRight 眼睛节点
  3. 动态创建 Sprite2D 加载 `assets/wanjia.png`，按高度 36px 等比缩放
  4. 应用 `wanjia_black_transparent.gdshader` 剔除纯黑背景（wanjia.png 有黑色背景）
  5. `BotGame.gd` 和 `ZombieGame.gd` 的 `_ready()` 中设置 `player.use_custom_skin = true`
- 联机模式未修改（MultiplayerPlayer 使用 MultiplayerPlayer.gd，不受影响）
- **状态: 已恢复默认圆形角色**（BotGame.gd / ZombieGame.gd 中调用代码已注释，Player.gd 中 `use_custom_skin` 默认 false）

## 修复: GDScript for name shadowing 警告 + 联机昵称永久保存
- 时间: 16:58
- 文件: `scripts/Player.gd`, `Autoload/GameSettings.gd`, `scripts/LobbyUI.gd`

### 1. for name shadowing
- `Player.gd` 第680行: `for name in eye_names:` → `for eye_name in eye_names:`（变量名 `name` 与 `Node.name` 属性冲突）

### 2. 联机昵称永久保存
- `GameSettings.gd` DEFAULTS 中 game 节添加 `"nickname": ""`
- `LobbyUI.gd` `_build_create_screen` / `_build_join_screen`: 创建昵称输入框后自动读取 `GameSettings.get_value("game", "nickname", "")` 填入
- `LobbyUI.gd` `_on_create_room_pressed` / `_on_join_room_pressed`: 创建/加入房间成功后调用 `GameSettings.set_value("game", "nickname", nick)` 保存到本地 `user://settings.cfg`
- 效果: 首次输入昵称后，以后打开游戏创建/加入房间时自动填充，无需重复输入

### 3. 测试人员特殊ID彩蛋
- `LobbyUI.gd`: 添加常量 `TESTER_ID = "19992884240891841"` 和 `TESTER_NAME = "测试人员 ★"`
- 新增 `_resolve_nickname(raw)` 函数：输入匹配 TESTER_ID 时返回 TESTER_NAME
- 在 `_on_create_room_pressed` 和 `_on_join_room_pressed` 中调用 `_resolve_nickname`
- 效果: 输入特殊ID后，游戏中显示为 "测试人员 ★"，但本地保存的仍是原始输入，下次自动填充

## 修复: 首次游戏昵称设置窗口移至 Intro 动画结束
- 时间: 17:15
- 文件: `scripts/Intro.gd`, `scripts/LobbyUI.gd`

### 问题
- 用户反馈大厅里的"首次游戏"提示没用，且一直显示
- 用户要求：在 intro 动画结束时弹出昵称设置

### 修复
1. `LobbyUI.gd`: 删除 `_build_create_screen` 和 `_build_join_screen` 中的首次游戏提示（红色"首次游戏，请设置您的昵称" Label）
2. `Intro.gd`: 
   - `_transition_to_menu()` 和 `_skip_to_menu()` 中添加检查：`GameSettings.get_value("game", "nickname", "")`
   - 如果为空，调用 `_show_nickname_setup()` 弹出设置窗口，而不是直接跳转主菜单
   - 新增 `_show_nickname_setup()` 函数：创建全屏半透明遮罩 + 中央面板
   - 面板包含：标题、LineEdit 输入框（最多12字符）、"确定"按钮、"使用默认"按钮
   - 确定：保存昵称 → 跳转到主菜单
   - 使用默认：保存"玩家" → 跳转到主菜单
   - 回车键也可触发确认
3. 修复弹窗居中：用 `CenterContainer` 包裹 `PanelContainer`，确保绝对屏幕正中间（原 `PRESET_CENTER` 在 Node2D 父节点下偏移）
4. 大厅昵称只读化：`LobbyUI.gd` 创建/加入房间界面的 `nickname_input` 和 `join_nickname_input` 设置 `editable = false`，去掉 placeholder，仅显示已保存昵称，不可修改
- 状态: 已修复

## 新功能: 联机单挑25人头胜利限制 + 房间模式切换 + 官网更新 (2025-06-30)
### 1. 联机单挑模式25人头限制
- 文件: `scenes/game/multiplayer/DuelGame.gd`
- 添加队伍击杀计数: `red_kills`, `blue_kills`, `DUEL_KILL_LIMIT = 25`
- 本地玩家击杀远程玩家 → 本地玩家所属队伍 +1
- 远程玩家击杀本地玩家 → 对方队伍 +1（单挑1v1中即远程玩家队伍）
- `_check_win_condition()`: 任一队伍达到25时触发游戏结束
- `_show_victory_panel()`: 显示"你胜利！" + 比分 + "返回房间"按钮
- `_show_defeat_panel()`: 显示"失败" + 比分 + "返回房间"按钮
- `_on_return_to_room()`: 点击后回到联机等待房间（保持网络连接），重置准备状态
- 游戏结束后暂停菜单和死亡面板不再显示
- 暂停菜单中增加"返回房间"选项

### 2. 联机等待房间支持切换模式
- 文件: `Autoload/NetworkManager.gd`
- 新增 `reset_ready()`: 重置所有玩家准备状态，RPC同步
- 新增 `change_room_mode(new_mode)`: 房主切换房间模式，循环 DUEL→BRAWL→ZOMBIE→DUEL
- 新增 `room_mode_changed` 信号 + `_sync_room_mode` RPC（房主切换后同步给客户端）
- 切换模式后清空所有颜色分配，重新分配房主默认颜色，更新广播信息
- 文件: `scripts/LobbyUI.gd`
- `_ready()` 中检查 `NetworkManager.connected`，已连接则直接显示等待界面（支持游戏结束后返回）
- `_build_waiting_screen()`: 添加 `mode_switch_btn` 模式切换按钮（仅房主可见）
- `_on_mode_switch_pressed()`: 房主循环切换模式，调用 `NetworkManager.change_room_mode()` 并 `reset_ready()`
- 连接 `room_mode_changed` 信号，收到后自动刷新等待界面

### 3. 官网更新
- 文件: `website/index.html`
- 联机对战卡片描述更新：加入"单挑1v1（先达到25击杀的队伍获胜）"和"房间内可随时切换模式"
- 特色功能第6项"低延迟联机"改为"联机对战系统"，详细描述单挑25击杀、乱斗、僵尸合作、房间切换模式、踢人、准备确认、自由选色等功能
- 已重新部署: https://d6eee82701bd4c86a83f8a48695a85c5.app.codebuddy.work
- 翻译新增: `victory`, `defeat`, `return_room`, `duel_score_format`, `first_to_25`, `game_mode`, `switch_mode`, `mode_changed`

## 更新: 游戏内游戏介绍面板
- 时间: 18:16
- 文件: `Autoload/GameSettings.gd`, `scripts/MainMenu.gd`
- 修改内容:
  1. **僵尸模式描述更新**: BOSS血量从 2000 修正为 5000，加入"还会扔手雷"和"僵尸联机合作"说明
  2. **武器介绍更新**: 加入"刀 — 近战武器，挥刀有扇形冰蓝色拖尾特效，按 4 切换"
  3. **操作说明更新**: 加入"4 — 刀"和"M — 切换小地图显示"
  4. **新增联机模式卡片**: 在 Bot 模式卡片后添加联机模式介绍卡片（蓝色主题），包含三种模式详细说明：单挑25人头、乱斗、僵尸合作（观战、队友面板等）
  5. **新增翻译键**: `about_multiplayer_title`, `about_multiplayer_desc`
## 修复: GameSettings.gd 重复翻译键 `switch_mode`
- 时间: 18:26
- 原因: 第142行和第224行各定义了一次 `switch_mode`，导致 GDScript 字典解析报错
- 修复: 删除第224行的重复项

## 官网更新: 导航栏logo + 关于我们头像签名
- 时间: 18:34
- 修改内容:
  1. **导航栏**: 去掉 `logo_dark.svg`（Godot图标），改为纯文字 "BULLET RUN"（金色渐变，Orbitron字体，字间距3px）
  2. **关于我们**: 右侧替换为创始人卡片（头像 + 名字 + 创始人标签 + 签名图）
- 新增素材: `website/assets/qiudidia-signature.png`
- 预览: https://a501aac9f7454bca8da1d30e79c7ea1d.app.codebuddy.work
- 状态: 已部署

## 官网修正: 年份+下载链接泄露+联系我们板块
- 时间: 19:59
- 修改内容:
  1. **年份修正**: `2025.07.11` → `2026.07.11`，`© 2025` → `© 2026`
  2. **下载链接泄露修复**: 页脚"下载页面"直接链接蓝奏云 → 删除；Hero 区"蓝奏云下载"改为"7.11 正式上线"
  3. **新增联系我们板块**: 三张卡片（QQ 3257717475 / 微信 qiudidia114514 / 邮箱 bulletrun@foxmail.com），每张带SVG图标+复制按钮
  4. 导航栏增加"联系我们"链接
  5. `main.js` 新增 `copyText()` 通用复制函数
- 网站部署平台: GitHub Pages (用户自行部署，域名 bulletrun.github.io)
- 状态: 已完成

## 新功能: 装备配置系统 + 机枪武器 + 特长系统 (续)
- 时间: 16:31 (续上次中断)
- 文件: `scripts/MainMenu.gd`
- 完成内容: 在游戏关于界面添加特长系统卡片
  - 紫色主题 PanelContainer (bg: 0.10,0.06,0.14, border: 紫色0.8,0.4,0.9)
  - 包含特长标题(PerksTitle, 22px紫色)和特长描述(PerksDesc, 14px灰色)
  - `_refresh_about_panel()` 中读取 `about_perks_title` 和 `about_perks_desc` 翻译键
  - 位置: 武器介绍之后、分隔线之后、操作说明之前
- 状态: 已完成
