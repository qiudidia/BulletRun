# 2026-06-21 工作记录

## Bullet Run - 僵尸模式无限波次、ESC退出、各类bug修复

### 本次修改清单

- **无限波次**：`ZombieGame.gd` 去掉 `max_wave = 20` 限制，删掉 `_game_won()`，改为一直打直到玩家死亡
- **ESC退出（僵尸模式）**：`zombie_game.tscn` 加 `ExitConfirmPanel`，`ZombieGame.gd` 加 `_input()` 检测 `ui_cancel`，暂停后弹出确认面板（确认退出/取消继续）
- **ESC退出（Bot模式）**：`bot_game.tscn` 加 `ExitConfirmPanel`（format=3→4），`BotGame.gd` 加同样的 ESC 检测逻辑
- **死亡界面**：`zombie_game.tscn` 加 `DeathPanel`（含波次信息、最高波次、再开一局/返回主菜单）
- **删掉 VictoryPanel**：无限波次不需要通关界面，从 .tscn 和 .gd 里全部移除
- **`_check_wave_complete()` bug修复**：加 `await get_tree().process_frame` 等待 `queue_free()` 完成，避免波次卡死
- **僵尸刷新位置修复**：`_spawn_wave_enemies()` 和 `_spawn_temp_zombie()` 限制在视口内，加 `get_viewport_rect()` 保护
- **准星跟随修复**：`Crosshair.gd` 的 `PROCESS_MODE_WHEN_PAUSED` → `PROCESS_MODE_ALWAYS`
- **`VeeIcon.gd` 警告修复**：`angle_deg` → `_angle_deg`
- **`_game_over()` SceneTreeTimer bug修复**：去掉 `t.process_mode` 赋值
- **`intro.tscn` 解析错误修复**：`format=3` → `format=4`，去掉假UID

### 技术细节

- ESC退出用独立 `ExitConfirmPanel`，按钮连接固定不来回改，避免信号连接bug
- 死亡/退出确认面板都挂在 `UI` (CanvasLayer) 下，`process_mode = ALWAYS` 确保暂停时按钮可点
- 僵尸模式 `_on_restart_pressed()` 用 `get_tree().reload_current_scene()` 重新加载场景
- Bot模式临时Bot创建时正确设置 CollisionShape2D 的 shape 再 add_child
